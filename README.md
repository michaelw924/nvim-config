# Michael's Neovim Config

Portable Neovim configuration based on kickstart.nvim.

## Install (Linux)

Run:

```bash
curl -fsSL https://raw.githubusercontent.com/michaelw924/nvim-config/install.sh | bash
